version: "3.5"

services:
  app:
    image: strm/dnsmasq
    container_name: dnsmasq
    restart: always
    volumes:
      - /share/docker/dnsmasq/app/dnsmasq.conf:/etc/dnsmasq.conf
    ports:
      - "53:53/udp"
    cap_add:
      - NET_ADMIN